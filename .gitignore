# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
**/doc/api/

# IDE
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json

# Android local SDK path (machine-specific)
**/android/local.properties

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env & secrets
.env
.env.*
!.env.example

# Local data (dev)
data/
logs/
*.db
*.db-shm
*.db-wal

# FVM
.fvm/flutter_sdk

# Coverage
coverage/
lcov.info

# Native / Rust (future p2p crate)
target/
**/generated/

# Bundled libp2p FFI artifacts (built via scripts/build-native-ffi.*)
apps/meshpad/windows/runner/native/*.dll
apps/meshpad/linux/runner/native/*.so
apps/meshpad/linux/runner/native/*.dylib
apps/meshpad/android/app/src/main/jniLibs/**/libmeshpad_p2p_native.so

# Web server runtime (Linux headless)
var/

# Downloaded VS bootstrapper (scripts/install-vs-atl.ps1)
tools/*.exe
