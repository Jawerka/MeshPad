# libp2p FFI native libraries (Android, archived)

> **Archived** — not built in CI/release (ADR 0003). See [docs/LIBP2P.md](../../../../../../../docs/LIBP2P.md).

`libmeshpad_p2p_native.so` per ABI would be produced by `scripts/build-native-ffi-android.sh`. Not committed to git.
