# Release signing — run scripts/setup-android-signing.ps1 (creates this file + GitHub secrets).
# CI requires the same keystore via ANDROID_KEYSTORE_* secrets.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=meshpad
storeFile=../meshpad-release.keystore
