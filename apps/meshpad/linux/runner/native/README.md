# Bundled libp2p FFI (Linux, archived)

> **Archived** — not built in CI/release (ADR 0003). See [docs/LIBP2P.md](../../../../docs/LIBP2P.md).

`libmeshpad_p2p_native.so` would be produced by `scripts/build-native-ffi.sh`. Not committed to git.
