# Bundled libp2p FFI (Windows, archived)

> **Archived** — not built in CI/release (ADR 0003). See [docs/LIBP2P.md](../../../../docs/LIBP2P.md).

`meshpad_p2p_native.dll` would be produced by `scripts/build-native-ffi.ps1`. Not committed to git.
