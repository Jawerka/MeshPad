name: meshpad_core
description: Domain, file storage, and sync logic for MeshPad (pure Dart).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  crypto: ^3.0.6
  drift: ^2.22.1
  image: ^4.5.4
  meta: ^1.15.0
  path: ^1.9.0
  sqlite3: ^2.4.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.22.1
  lints: ^5.0.0
  test: ^1.25.0
