name: meshpad_p2p
description: P2P sync transport abstraction for MeshPad.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  basic_utils: ^5.7.0
  crypto: ^3.0.6
  mdns_dart: ^2.2.1
  meshpad_core:
    path: ../meshpad_core
  meta: ^1.15.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  shelf: ^1.4.2
  meshpad_p2p_sidecar:
    path: ../../native/meshpad_p2p_sidecar
  test: ^1.25.0
